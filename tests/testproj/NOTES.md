# Test project

A tiny Lua project the smoke test runs the tools against.

## Layout

Sources live under `lua/testproj`.

### Modules

- `main.lua` calls into `util.lua`.
- `messy.lua` exists to have diagnostics.

## Running

Nothing to run; the smoke test loads the files.
